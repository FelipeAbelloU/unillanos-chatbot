# =============================================================
# Variables de entorno del Asistente Normativo Unillanos
# Copia este archivo como ".env" y rellena los valores.
# El archivo .env NUNCA debe subirse a git.
# =============================================================

# --- Backend OpenAI-compatible (opcional) ---
# Necesario solo si usas backend: openai en config/config.yaml
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# --- Ollama (opcional, solo si cambias el puerto por defecto) ---
# OLLAMA_BASE_URL=http://localhost:11434
